<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>_AdminLayout</title>
</head>
<body>
    <div class="pagina">
        @RenderSection("cabecalho")
        @RenderPage("~/Views/Shared/_MenuAdmin.cshtml")
    </div>
    <div id="principal">
        @RenderBody()
    </div>
    <div>
        @if (IsSectionDefined("rodape"))
        {
            @RenderSection("rodape")
        }
    </div>
</body>
</html>
